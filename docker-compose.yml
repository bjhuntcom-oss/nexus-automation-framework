version: '3.8'

services:
  nexus-automation-framework:
    build: .
    container_name: nexus-automation-framework
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./sessions:/app/sessions
      - ./reports:/app/reports
      - ./loot:/app/loot
      - ./downloads:/app/downloads
      - ./knowledge.db:/app/knowledge.db
    environment:
      - PYTHONUNBUFFERED=1
      - NEXUS_LOG_LEVEL=INFO
      - NEXUS_STRATEGIC_ENGINE_ENABLED=true
      - NEXUS_OBSERVABILITY_ENABLED=true
      - NEXUS_GOVERNANCE_KEY=nexus-automation-framework-production-key-change-in-production
    networks:
      - nexus-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Optional: Prometheus for metrics collection
  prometheus:
    image: prom/prometheus:latest
    container_name: nexus-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    networks:
      - nexus-network
    restart: unless-stopped
    profiles:
      - monitoring

  # Optional: Grafana for visualization
  grafana:
    image: grafana/grafana:latest
    container_name: nexus-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_USERS_ALLOW_SIGN_UP=false
    networks:
      - nexus-network
    restart: unless-stopped
    profiles:
      - monitoring

  # Optional: Redis for distributed orchestration
  redis:
    image: redis:7-alpine
    container_name: nexus-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    networks:
      - nexus-network
    restart: unless-stopped
    profiles:
      - distributed

  # Optional: PostgreSQL for audit logs and governance data
  postgres:
    image: postgres:15-alpine
    container_name: nexus-postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./monitoring/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      - POSTGRES_DB=nexus_framework
      - POSTGRES_USER=nexus
      - POSTGRES_PASSWORD=nexus_password_change_in_production
    networks:
      - nexus-network
    restart: unless-stopped
    profiles:
      - enterprise

volumes:
  prometheus-data:
  grafana-data:
  redis-data:
  postgres-data:

networks:
  nexus-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
